# Module cache

Root module pulls several small child modules (`mod_null`, `mod_random`, `mod_time`, `mod_tls`, `mod_terraform_data`) to populate the module/provider cache. Use when testing init cache behavior and cold vs warm runs.
